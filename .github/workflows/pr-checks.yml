name: pr-checks

# S9b (app-ux-overhaul repo split): this workflow lives in the CONTENT repo
# (genie-spaces-content) because GitHub Actions only fires on events in the repo that CONTAINS
# the workflow file (RS2). But the pipeline LOGIC (render.sh, check_grants.py, databricks.yml,
# tests) lives in the app repo (genie-promote-cicd) and is NOT duplicated here — instead each job
# checks out the app repo and OVERLAYS this repo's promotable content (src/) into it, so the
# unchanged engine scripts run exactly as they did pre-split (they're coupled to a working dir
# that has engine + content together). Byte-identical deterministic gate to the app repo's old
# pr-checks.yml, just with a checkout+overlay preamble.
on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths-ignore:
      - '**.md'
      - 'docs/**'

permissions:
  contents: read

env:
  DATABRICKS_BUNDLE_ENGINE: direct
  # The app (engine + scripts + databricks.yml) repo the pipeline logic is checked out from.
  APP_REPO: malcolndandaro/genie-promote-cicd

jobs:
  validate:
    name: bundle validate (prod)
    runs-on: self-hosted   # workspace IP-ACL blocks hosted runners
    if: vars.DATABRICKS_PROD_HOST != ''
    env:
      DATABRICKS_HOST: ${{ vars.DATABRICKS_PROD_HOST }}
      DATABRICKS_AUTH_TYPE: oauth-m2m
      DATABRICKS_CLIENT_ID: ${{ vars.DATABRICKS_PROD_SP_CLIENT_ID }}
      DATABRICKS_CLIENT_SECRET: ${{ secrets.DATABRICKS_PROD_SP_SECRET }}
    steps:
      # 1. The app repo = the engine + all pipeline logic (render.sh, check_grants.py, tests,
      #    databricks.yml). Checked out at the workspace root so every script runs from a dir that
      #    looks exactly like the pre-split single repo.
      - name: Checkout app repo (pipeline logic)
        uses: actions/checkout@v4
        with:
          repository: ${{ env.APP_REPO }}
          path: app
      # 2. The content repo = the PR under test (this repo's promotable src/).
      - name: Checkout content (this PR)
        uses: actions/checkout@v4
        with:
          path: content
      # 3. Overlay this PR's content into the app-repo working tree, so render.sh finds src/genie/*
      #    where it expects. --delete so a removed content file doesn't linger from the app checkout.
      - name: Overlay content src/ into the app working tree
        run: rsync -a --delete content/src/ app/src/
      - uses: databricks/setup-cli@main
        with:
          version: 1.4.0
      - name: Unit tests (engine + engine API)
        working-directory: app
        run: |
          python3 -m pip install -q 'databricks-sdk>=0.111.0' fastapi uvicorn httpx pytest
          python3 -m pytest tests/ -q
      - name: Pre-render + identifier allowlist (rejects foreign-env / cross-domain refs)
        working-directory: app
        run: bash scripts/render.sh prod
      - name: Stub the app source dir (ADR-0006/A1 — promote_app now targets prod)
        working-directory: app
        run: mkdir -p build/promote_app
      - name: Validate bundle (prod target)
        working-directory: app
        run: databricks bundle validate -t prod --var "warehouse_id=${{ vars.DATABRICKS_PROD_WAREHOUSE_ID }}"
      - name: GRANT-01 — every declared principal can SELECT the prod tables (governance gate)
        working-directory: app
        run: |
          python3 -m pip install -q 'databricks-sdk>=0.111.0'
          for f in build/genie/*.serialized_space.json; do
            [ -f "$f" ] || continue
            slug="$(basename "$f" .serialized_space.json)"
            python3 scripts/check_grants.py "$f" "${{ vars.DATABRICKS_CONSUMER_GROUP || 'account users' }}" "build/genie/${slug}.access.json"
          done
      - name: EVAL-01 — the space carries enough benchmark questions (quality gate)
        working-directory: app
        # Deterministic COUNT gate (offline — reads the rendered JSON, no workspace creds): fails the
        # PR when a space has fewer than the required benchmark questions, so branch protection blocks
        # the merge. The full eval-RUN pass-rate can't run pre-merge (no live prod space + no dev creds
        # in CI), so it stays the app's own gate; this count is the merge-blocking floor.
        run: |
          for f in build/genie/*.serialized_space.json; do
            [ -f "$f" ] || continue
            python3 scripts/check_eval.py "$f"
          done
